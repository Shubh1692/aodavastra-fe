import React from 'react'
import { Box } from '@mui/material';
import Layout from '../../Components/Layout';

const SearchComponent = () => {
   
    return (
        <Box sx={{ marginTop: '8pc' }}>

        </Box>
    )
}
export default Layout(SearchComponent);